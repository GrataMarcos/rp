# HTTP para llamadas a Meta API
requests>=2.31.0

# OpenAI: GPT-4o-mini (parseo de gastos) + Whisper (transcripción de audio)
openai>=1.35.0

# Google Sheets API
google-auth>=2.29.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.130.0
httplib2>=0.22.0

# boto3 NO se incluye: ya viene en el runtime de Lambda
