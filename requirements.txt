# HTTP para llamadas a Meta API y Supabase REST
requests>=2.31.0

# OpenAI: GPT-4o-mini (parseo de gastos) + Whisper (transcripción de audio)
openai>=1.35.0

# boto3 NO se incluye: ya viene en el runtime de Lambda (se usa para dedup DynamoDB)
